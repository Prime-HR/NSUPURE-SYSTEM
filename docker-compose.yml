version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: nsupure_db
    restart: always
    environment:
      POSTGRES_USER: nsupure_admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set a unique database password}
      POSTGRES_DB: nsupure_enterprise
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nsupure_admin -d nsupure_enterprise"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: ../Dockerfile.backend
    container_name: nsupure_api
    restart: always
    environment:
      PORT: 5000
      NODE_ENV: production
      DATABASE_URL: "${DATABASE_URL:?Set the persistent PostgreSQL connection URL}"
      JWT_SECRET: "${JWT_SECRET:?Set a unique signing secret}"
      JWT_EXPIRES_IN: "7d"
      UPLOAD_DIR: "/app/uploads"
    ports:
      - "5000:5000"
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: ../Dockerfile.frontend
    container_name: nsupure_web
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  uploads_data:
